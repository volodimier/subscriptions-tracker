# Testcontainers configuration for CI environments
# Disable container reuse in CI (requires local .testcontainers.properties which isn't present in CI)
testcontainers.reuse.enable=false

# Increase timeouts for CI environments where Docker might be slower
startup.timeout=60
